---
import Footer from "../components/sections/Footer.tsx";
import Navbar from "../components/sections/Navbar.tsx";
---

<html lang="es">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <meta name="generator" content={Astro.generator} />

        <!-- SEO Meta Tags -->
        <title>
            Brevana - Refugio Costero Auténtico en Miches, República Dominicana
        </title>
        <meta
            name="description"
            content="Brevana es un refugio costero auténtico en Miches donde el mar Caribe se encuentra con el río Yeguada. Experiencias culturales, gastronomía dominicana y tradiciones como la mamajuana en un ambiente familiar."
        />
        <meta
            name="keywords"
            content="Brevana, Miches, República Dominicana, mar Caribe, río Yeguada, mamajuana, refugio costero, experiencias auténticas, turismo cultural, Provincia El Seibo"
        />

        <!-- Open Graph Meta Tags -->
        <meta
            property="og:title"
            content="Brevana - Refugio Costero Auténtico en Miches"
        />
        <meta
            property="og:description"
            content="Donde el mar Caribe se encuentra con el río Yeguada. Experiencias auténticas, tradición de la mamajuana y hospitalidad dominicana en Miches."
        />
        <meta property="og:type" content="website" />
        <meta property="og:url" content="https://brevana.com" />
        <meta property="og:image" content="https://brevana.com/og-image.jpg" />

        <!-- Twitter Card Meta Tags -->
        <meta name="twitter:card" content="summary_large_image" />
        <meta
            name="twitter:title"
            content="Brevana - Refugio Costero Auténtico en Miches"
        />
        <meta
            name="twitter:description"
            content="Refugio costero donde el mar Caribe se encuentra con el río Yeguada. Tradición de la mamajuana y experiencias auténticas en Miches."
        />
        <meta
            name="twitter:image"
            content="https://brevana.com/twitter-image.jpg"
        />

        <!-- Additional Meta Tags -->
        <meta name="robots" content="index, follow" />
        <meta name="author" content="Brevana" />
        <link rel="canonical" href="https://brevana.com" />

        <!-- Schema.org JSON-LD -->
        <script type="application/ld+json">
            {
                "@context": "https://schema.org",
                "@type": "Hotel",
                "name": "Brevana",
                "description": "Un refugio costero auténtico inspirado en la tradición de la mamajuana, donde el mar Caribe se encuentra con el río Yeguada en auténtica hospitalidad dominicana.",
                "address": {
                    "@type": "PostalAddress",
                    "streetAddress": "Carretera Miches – El Cedro",
                    "addressLocality": "Miches",
                    "addressRegion": "Provincia El Seibo",
                    "addressCountry": "República Dominicana"
                },
                "geo": {
                    "@type": "GeoCoordinates",
                    "latitude": "18.98675",
                    "longitude": "-69.039889"
                },
                "telephone": "+1-829-357-9841",
                "url": "https://brevana.com",
                "sameAs": ["https://instagram.com/rastudios_dr"],
                "amenityFeature": [
                    {
                        "@type": "LocationFeatureSpecification",
                        "name": "Acceso al Mar Caribe"
                    },
                    {
                        "@type": "LocationFeatureSpecification",
                        "name": "Actividades en Río Yeguada"
                    },
                    {
                        "@type": "LocationFeatureSpecification",
                        "name": "Experiencias con Mamajuana"
                    },
                    {
                        "@type": "LocationFeatureSpecification",
                        "name": "Gastronomía Dominicana"
                    },
                    {
                        "@type": "LocationFeatureSpecification",
                        "name": "Ambiente Familiar"
                    },
                    {
                        "@type": "LocationFeatureSpecification",
                        "name": "Música en Vivo"
                    }
                ],
                "starRating": {
                    "@type": "Rating",
                    "ratingValue": "4.5"
                }
            }
        </script>

        <!-- Smooth Scrolling CSS -->
        <style>
            html {
                scroll-behavior: smooth;
                font-family:
                    "Manrope",
                    system-ui,
                    -apple-system,
                    sans-serif;
            }

            /* Custom scrollbar */
            ::-webkit-scrollbar {
                width: 8px;
            }

            ::-webkit-scrollbar-track {
                background: oklch(0.94 0.004 0);
            }

            ::-webkit-scrollbar-thumb {
                background: oklch(0.64 0.18 80);
                border-radius: 4px;
            }

            ::-webkit-scrollbar-thumb:hover {
                background: oklch(0.56 0.16 75);
            }

            /* Loading animation */
            .page-loader {
                position: fixed;
                top: 0;
                left: 0;
                right: 0;
                bottom: 0;
                background: linear-gradient(
                    135deg,
                    oklch(0.64 0.18 80) 0%,
                    oklch(0.52 0.16 188) 100%
                );
                z-index: 9999;
                display: flex;
                align-items: center;
                justify-content: center;
                opacity: 1;
                transition: opacity 0.5s ease-out;
            }

            .page-loader.hidden {
                opacity: 0;
                pointer-events: none;
            }

            .loader-content {
                text-align: center;
                color: white;
            }

            .loader-logo {
                font-size: 4rem;
                font-weight: 800;
                margin-bottom: 1rem;
                animation: pulse 3s ease-in-out infinite;
                font-family: "Marcellus SC", ui-serif, serif;
            }

            .loader-text {
                font-size: 1rem;
                font-weight: 300;
                opacity: 0.8;
            }

            @keyframes pulse {
                0%,
                100% {
                    transform: scale(1);
                }
                50% {
                    transform: scale(1.05);
                }
            }
        </style>
    </head>
    <body class="antialiased">
        <!-- Page Loader -->
        <div id="page-loader" class="page-loader">
            <div class="loader-content">
                <div class="loader-logo">BREVANA</div>
            </div>
        </div>

        <Navbar client:load />
        <main class="max-w-[1440px] mx-auto px-8 md:px-16 lg:px-24">
            <slot />
        </main>
        <Footer client:visible />
        <script>
            // Page loader
            window.addEventListener("load", () => {
                const loader = document.getElementById("page-loader");
                setTimeout(() => {
                    loader?.classList.add("hidden");
                }, 1000);
            });

            // Smooth scroll for navigation links
            document.addEventListener("DOMContentLoaded", () => {
                const links = document.querySelectorAll('a[href^="#"]');
                links.forEach((link) => {
                    link.addEventListener("click", (e) => {
                        e.preventDefault();
                        const targetId = link.getAttribute("href");
                        const targetElement = document.querySelector(targetId);
                        if (targetElement) {
                            const offsetTop = targetElement.offsetTop - 80; // Account for fixed navbar
                            window.scrollTo({
                                top: offsetTop,
                                behavior: "smooth",
                            });
                        }
                    });
                });
            });

            // Add scroll-based animations
            const observerOptions = {
                threshold: 0.1,
                rootMargin: "0px 0px -50px 0px",
            };

            const observer = new IntersectionObserver((entries) => {
                entries.forEach((entry) => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add("animate-in");
                    }
                });
            }, observerOptions);

            // Observe all sections when DOM is loaded
            document.addEventListener("DOMContentLoaded", () => {
                const sections = document.querySelectorAll("section");
                sections.forEach((section) => {
                    observer.observe(section);
                });
            });
        </script>
    </body>
</html>
